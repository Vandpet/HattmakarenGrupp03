@model HattmakarenWebbAppGrupp03.Models.ViewModels.LoginViewModel

<link rel="stylesheet" href="~/css/LoginMobile.css" asp-append-version="true" />


@{
    ViewData["Title"] = "Logga in";
}
@if (ViewBag.NoUsersExist == true)
{
    <div class="alert alert-warning">
        Inga användare finns i databasen ännu.
        <a asp-controller="Employee" asp-action="SetupFirstUser" class="alert-link">
            Klicka här för att skapa första användaren
        </a>.
    </div>
}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-4">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h3 class="mb-0">Logga in</h3>
                </div>

                <div class="card-body">
                    <form asp-action="Login" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <div class="mb-3">
                            <label asp-for="Username" class="form-label">Användarnamn</label>
                            <input asp-for="Username" class="form-control" />
                            <span asp-validation-for="Username" class="text-danger"></span>
                        </div>

                        <div class="mb-4">
                            <label asp-for="Password" class="form-label">Lösenord</label>
                            <input asp-for="Password" class="form-control" />
                            <span asp-validation-for="Password" class="text-danger"></span>
                        </div>

                        <button type="submit" class="btn btn-primary w-100">Logga in</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
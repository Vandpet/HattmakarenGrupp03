@model IEnumerable<HattmakarenWebbAppGrupp03.Models.MaterialOrder>
<link rel="stylesheet" href="~/css/MaterialOrderMobile.css" asp-append-version="true" />


<h2>Material Orders</h2>

<a asp-controller="MaterialOrder" asp-action="Create" class="btn btn-success mb-3">
    New Material Order +
</a>

<table class="table table-hover">
    <thead>
        <tr>
            <th>ID</th>
            <th>Status</th>
            <th>Manage</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.MoId</td>
                <td>
                    @if (item.Printed)
                    {
                        <span class="badge bg-success">Ordered</span>
                    }
                    else
                    {
                        <span class="badge bg-secondary">Not Ordered</span>
                    }
                </td>
                <td>
                    @if (!item.Printed)
                    {
                        <form asp-controller="MaterialOrder" asp-action="MarkAsSent" method="post">
                            <input type="hidden" name="id" value="@item.MoId" />
                            <button type="submit" class="btn btn-sm btn-outline-success">
                                Mark as Ordered
                            </button>
                        </form>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>